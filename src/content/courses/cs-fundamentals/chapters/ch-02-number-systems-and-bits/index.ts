import type { Chapter } from '@/content/types'
import { createPlaceholderChapter } from '../placeholder'

export const ch02NumberSystemsAndBits: Chapter = createPlaceholderChapter(
  'ch-02-number-systems-and-bits',
  2,
  'Sistem Bilangan dan Operasi Bit',
  'Mempelajari desimal, biner, oktal, heksadesimal, operasi bitwise, bit shift, dan representasi bilangan negatif.'
)
